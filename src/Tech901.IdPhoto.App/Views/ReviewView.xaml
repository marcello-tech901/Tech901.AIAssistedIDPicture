<UserControl x:Class="Tech901.IdPhoto.App.Views.ReviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:Tech901.IdPhoto.App.Converters">
  <UserControl.Resources>
    <converters:ByteArrayToImageConverter x:Key="ByteArrayToImage" />
  </UserControl.Resources>

  <Grid>
    <Border Background="{StaticResource BackgroundGradientBrush}" />

    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
      <StackPanel.RenderTransform>
        <TranslateTransform />
      </StackPanel.RenderTransform>
      <StackPanel.Triggers>
        <EventTrigger RoutedEvent="Loaded">
          <BeginStoryboard>
            <Storyboard>
              <DoubleAnimation Storyboard.TargetProperty="Opacity"
                               From="0" To="1" Duration="0:0:0.5" />
              <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
                               From="20" To="0" Duration="0:0:0.6">
                <DoubleAnimation.EasingFunction>
                  <CubicEase EasingMode="EaseOut" />
                </DoubleAnimation.EasingFunction>
              </DoubleAnimation>
            </Storyboard>
          </BeginStoryboard>
        </EventTrigger>
      </StackPanel.Triggers>

      <TextBlock Text="How does this look?"
                 Style="{StaticResource HeadingTextStyle}"
                 Margin="0,0,0,32" />

      <!-- Photo frame with luminous border -->
      <Grid HorizontalAlignment="Center" Margin="0,0,0,40">
        <!-- Glow behind the photo -->
        <Border CornerRadius="14" Margin="-6">
          <Border.Background>
            <RadialGradientBrush>
              <GradientStop Color="#20A4CC27" Offset="0" />
              <GradientStop Color="#00A4CC27" Offset="1" />
            </RadialGradientBrush>
          </Border.Background>
        </Border>
        <!-- Photo surface -->
        <Border CornerRadius="12"
                BorderBrush="{StaticResource BorderSubtleBrush}"
                BorderThickness="1"
                Background="{StaticResource SurfaceBrush}"
                Padding="4">
          <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="40" ShadowDepth="6" Opacity="0.6" />
          </Border.Effect>
          <Image Source="{Binding CapturedImage, Converter={StaticResource ByteArrayToImage}}"
                 MaxWidth="480" MaxHeight="480"
                 Stretch="Uniform"
                 RenderOptions.BitmapScalingMode="HighQuality">
            <Image.Clip>
              <RectangleGeometry Rect="0,0,480,480" RadiusX="8" RadiusY="8" />
            </Image.Clip>
          </Image>
        </Border>
      </Grid>

      <!-- Buttons -->
      <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
        <Button Content="Use This Photo"
                Style="{StaticResource PrimaryButtonStyle}"
                Command="{Binding AcceptCommand}"
                Margin="0,0,20,0" />
        <Button Content="Retake"
                Style="{StaticResource WarningButtonStyle}"
                Command="{Binding RetakeCommand}" />
      </StackPanel>
    </StackPanel>
  </Grid>
</UserControl>
